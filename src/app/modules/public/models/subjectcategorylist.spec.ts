import { Subjectcategorylist } from './subjectcategorylist';

describe('Subjectcategorylist', () => {
  it('should create an instance', () => {
    expect(new Subjectcategorylist()).toBeTruthy();
  });
});
